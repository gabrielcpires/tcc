<?php $usuario = $this->view->usuario; ?>
<div class="container-form">
    <div class="form-perfil">
        <form class="custom-form" enctype="multipart/form-data" method="post" action="/atualizarPerfil">
            <div class="form-group personal-image">
                <label for="avatar" class="label-avatar">
                    <input type="file" name="avatar" id="avatar" value="<?php $usuario[0]['pathUsuario']; ?>" />
                    <figure class="personal-figure">
                        <img src="<?php echo "/" . $usuario[0]['pathUsuario']; ?>" class="personal-avatar" alt="avatar">
                    </figure>
                </label>
            </div>
            <div class="form-group">
                <label for="nome">Nome:</label>
                <input type="text" class="form-control" name="nome" id="nome" value="<?php echo $usuario[0]['nome']; ?>"
                    required>
            </div>

            <div class="form-group">
                <label for="email">Email:</label>
                <input type="email" class="form-control" name="email" id="email"
                    value="<?php echo $usuario[0]['email']; ?>" disabled>
            </div>

            <div class="form-group">
                <label for="senha">Senha:</label>
                <input type="password" class="form-control" name="senha" id="senha"
                    value="<?php echo $usuario[0]['senha']; ?>" disabled>
            </div>

            <div class="form-group">
                <label for="estado">Selecione o Estado:</label>
                <select class="form-select" name="estado" id="estado" value="2">
                    <?php foreach ($this->view->estados as $estado) { ?>
                        <option value="<?php echo $estado->id ?>">
                            <?php echo $estado->nome_cidade; ?>
                        </option>
                    <?php } ?>
                </select>
            </div>

            <div class="l2">
                <button type="submit" class="btn btn-primary">Atualizar</button>
            </div>
        </form>
        <form action="/sair">
            <div class="l2-sair">
                <button type="submit" class="btn btn-primary">Desconectar</button>
            </div>
        </form>
    </div>
</div>