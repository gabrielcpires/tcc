<div class="container">
    <div class="row d-flex justify-content-center align-items-center" style="min-height: 100vh;">
        <div class="col-lg-8 col-md-10 col-sm-12 tweetBox" style="max-width: 600px;">
            <form class="tweet-form" enctype="multipart/form-data" method="post" action="/publicar">
                <div class="mb-3">
                    <label class="form-label" for="titulo">Título:</label>
                    <input class="form-control rounded-pill form-publicacao" type="text" name="titulo" id="titulo">
                </div>
                <div class="mb-3">
                    <label class="form-label">Texto:</label>
                    <textarea class="form-control rounded form-publicacao" name="texto" id="exampleFormControlTextarea1"
                        rows="3"></textarea>
                </div>
                <div class="mb-3">
                    <label class="form-label">Selecione o Estado:</label>
                    <select class="form-select" name="estado" id="estado">
                        <?php foreach ($this->view->estados as $estado) { ?>
                            <option value="<?php echo $estado->id ?>" name="estado"><?php echo $estado->nome_estado; ?>
                            </option>
                        <?php } ?>
                    </select>
                </div>
                <div class="mb-3">
                    <label class="form-label" for="arquivo">Selecione o arquivo</label>
                    <input class="form-control" type="file" name="arquivo" id="arquivo">
                </div>
                <div class="d-grid gap-2">
                    <button type="submit" class="btn btn-primary">Publicar</button>
                </div>
                <div class="personal-image">
                    <label class="label">
                        <input type="file" />
                        <figure class="personal-figure">
                            <img src="https://avatars1.githubusercontent.com/u/11435231?s=460&v=4"
                                class="personal-avatar" alt="avatar">
                            <figcaption class="personal-figcaption">
                                <img
                                    src="https://raw.githubusercontent.com/ThiagoLuizNunes/angular-boilerplate/master/src/assets/imgs/camera-white.png">
                            </figcaption>
                        </figure>
                    </label>
                </div>
            </form>
        </div>
    </div>
</div>